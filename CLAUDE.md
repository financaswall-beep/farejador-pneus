# CLAUDE.md — Memória do projeto Farejador

> Este arquivo é lido AUTOMATICAMENTE pelo Claude Code ao abrir o projeto (por qualquer conta/login).
> É a memória COMPARTILHADA do projeto. O detalhe histórico vive em `docs/SESSAO_*_HANDOFF.md`.
> Mantenha enxuto: só o durável. Atualize quando uma decisão grande mudar.

---

## 1. O que é
Rede de **borracharias** na região metropolitana do Rio. Um **bot no WhatsApp** (via Chatwoot) atende o cliente, descobre o pneu, e **roteia pra loja parceira mais perto** — pra **retirada** ou **entrega** (frete fixo R$9,90) — reservando o pneu. O dono (Wallace) é a **matriz**; as lojas são **parceiras** (pagam comissão).

- **Repo:** github.com/financaswall-beep/farejador-pneus
- **Supabase:** projeto **Farejador** (`aoqtgwzeyznycuakrdhp`), envs `prod` e `test`.
- **Deploy:** push pro `main` → **Wallace aperta Deploy no Coolify** (palavra do dono 06-11: NÃO sobe sozinho; registros antigos diziam "automático" — não confiar). Pós-deploy, conferir DE FORA que subiu: `curl` no painel (`farejador.smarttecsolutions.com.br/parceiro/<slug>/`) e comparar etiqueta `?v=`/hash com o main (provado 06-11: prod == main byte a byte).

## 2. Arquitetura (camadas)
- **Parceiro** — painel + banco do parceiro (vendas, estoque, clientes, financeiro/caixa, chat, entregas, retiradas). O DB é do PARCEIRO (tabelas próprias). `src/parceiro/`, `parceiro/public/`.
- **Matriz** — camada que AGREGA os parceiros: cobrança, comissão, funil da Rede, antifraude, candidaturas. `src/admin/`, `src/app/preview-matriz-server.ts`.
- **Bot / atendente-v2** — o agente conversacional (LLM via OpenAI) que atende no Chatwoot, busca produto/frete, cria/consulta pedido, e roteia bot→parceiro→matriz. `src/atendente-v2/`.
- **Ingestão** — Chatwoot → Supabase (webhooks, normalização, persistência). `src/webhooks/`, `src/normalization/`, `src/persistence/`.

## 3. Como mexer (convenções — IMPORTANTE)
- **Migration ANTES do push** (o código novo lê a coluna nova; aplicar via apply_migration). Migrations em `db/migrations/` (já aplicadas até 0098).
- **Test-first** em zona sensível. Provas: `npm run typecheck`, `npm test` (vitest, ~400 testes), e a integração do motor: `npx tsx --env-file=.env scripts/prova-geo-rede-test.ts` (env test, BEGIN/ROLLBACK). Banco direto dá ENOTFOUND em rede sem IPv6 → usar `--env-file=.env.pooler` (pooler IPv4).
- **Flags** pra mudança arriscada de roteamento (ex.: ROUTING_GEO, PICKUP_TO_PARTNER, ROUTING_GEO_ROAD_DISTANCE, ROUTING_MULTI_CANDIDATE, ROUTING_FAIRNESS). Sobe dormente, liga quando provar.
- **Chave do Google só vive no Coolify** (não no `.env` local) → reverse-geocode/distância de rua NÃO roda local; testar AO VIVO ou via `scripts/testar-geocode.cjs` (pode dar REQUEST_DENIED local se a chave for restrita por IP).
- **Comportamento crítico do bot se garante por CÓDIGO, não por prompt.** Pedir no prompt é probabilístico e falha; quando precisar garantir, force no código (ex.: o nudge determinístico do pino em `agent.ts`).
- `.env` tem FAREJADOR_ENV=test → `checar-naoregressao` SEM `--env-file` dá falso "regressão".
- **Dinheiro/estoque é contrato** (migrations 0076 reservado, 0077 financeiro): mexer com cuidado, snapshot de rollback, auditoria. Ver `SECOES/ESTOQUE.md`.
- **Teto de 300 linhas por arquivo de PRODUÇÃO — repo inteiro** (obra 300, 2026-07-05). Fiscal `npm run checar-tamanho` vigia `src/**/*.ts` + `painel/public/*.js` + `parceiro/public/*.js`: arquivo NOVO >300 = FALHA (nasce fatiado; corte por ASSUNTO, nunca tesourada na linha 300); os 14 gordos HERDADOS têm teto CONGELADO em `scripts/teto-herdado.json` (podem encolher, não engordar; quem baixar de 300 remove a entrada no mesmo commit). Fora da regra de propósito: tests/, scripts/ (prova é roteiro linear), migrations, *.html/*.css. Censo: `docs/CENSO_TAMANHO_ARQUIVOS_2026-07-05.md`.
- **Painéis fatiados em módulos-fábrica** (parceiro 2026-06: `app.js` 4.755→263 em 24 módulos `window.PARCEIRO_MODULES`; matriz 2026-07-05: `app.js` 3.002→249 em 21 módulos `window.PAINEL_MODULES`). Montagem via `getOwnPropertyDescriptors` — NUNCA spread (congela getter; a matriz hoje não tem getter, mas o molde blindado fica). Ao tocar painel do parceiro: `npm run prova-painel`; ao tocar painel da matriz: `node scripts/prova-paridade-matriz.cjs` (307 propriedades vs baseline — mudou a interface DE PROPÓSITO? regravar com `--gravar-baseline` no mesmo commit). Receita: `docs/PLANO_REFATORACAO_PAINEL_300_2026-06-10.md` + `docs/SESSAO_2026-07-05_OBRA_300_FISCAL_E_PAINEL_MATRIZ_HANDOFF.md`.

## 4. Como trabalhar com o Wallace (estilo — SEGUIR)
- Fala **pt-BR carioca**, direto. Ele é dono de loja, visão arquitetural clara, mas **leigo em código**.
- Seja **crítico e direto, não concordância automática** — ele quer que eu discorde quando tenho razão, e compara conselhos entre IAs. Aponte riscos, não só elogie.
- **Decisões técnicas ele delega a mim** (posso recrutar agentes). Só trago OPÇÃO quando é decisão DELE: **negócio, dinheiro, irreversível** — e em **linguagem de leigo**.
- Ao fim de cada resolução, **assinar** qual especialista do time fez + o modelo (ex.: "— Orquestrador (Claude Opus 4.8) — domínio `bot`").
- **Não derrubar** preview/recursos em execução por conta própria; deixar de pé e avisar (porta/id).
- Vocabulário: falar **"motor por cidade / por bairro"**, nunca "motor v1/v2" (v2 é a geração atual do atendente).
- Delegar ao Sonnet só se eu julgar que dá conta; zona sensível/dúvida fica no Opus.

## 5. Time de agentes (`.claude/agents/` — LOCAL, gitignored)
`bot` e `parceiro` e `matriz` e `banco` e `seguranca` rodam em Opus (zona sensível); `coletor`/`escriba`/`executor`/`front` em Sonnet (tarefas mecânicas). Use o especialista do domínio. ⚠️ Essa pasta NÃO está no git — não viaja pra outra máquina (só pro mesmo PC).

### 5.1 🔒 TRAVA — skills de design de terceiros (`ui-ux-pro-max` & cia) OFF neste projeto
As 7 skills de UI/UX instaladas GLOBAL na máquina do Wallace (`~/.claude/skills/`: `ui-ux-pro-max`, `ui-styling`, `design`, `design-system`, `brand`, `slides`, `banner-design`) são **PROIBIDAS de uso automático no Farejador**. Elas falam React/shadcn/Tailwind; nossos painéis são **Alpine.js + CSS puro** — o conselho de componente/config delas NÃO encaixa e pode introduzir padrão errado. Regra pra QUALQUER Claude/LLM que abrir este repo: **NÃO invocar nenhuma dessas skills a menos que o Wallace peça explicitamente** (ex.: "usa a skill de design nessa aba"). Sem pedido dele = trabalhar do jeito da casa (o stack do painel, §3). São ferramenta pessoal dele, não do projeto — o repo não depende delas e não as versiona.

## 6. O motor de roteamento (o coração)
`src/atendente-v2/fulfillment.ts` → `decideStoreForItemsGeo`:
1. **Candidatos** (`resolveUnitCandidates(municipio)`) ← hoje filtra por CIDADE (o "muro" — ver §8).
2. **Modo + cobertura** (`geo-routing.ts`: pickup ignora cobertura de bairro; entrega respeita).
3. **Estoque** (só loja que TEM o pneu, respeita `deleted_at`).
4. **Anel que cresce** (`ring.ts`): retirada FAIXAS [5,10,15] km, entrega [10,20,30,40] km. Banda mais perto ganha.
5. **Régua de justiça** (`rankUnitsByFairnessFromDb`): entre os do anel, ganha quem recebeu MENOS leads (anti-favorecimento). Nunca pula de banda.
- Coordenada do cliente: pino do WhatsApp (`getLatestCustomerLocation`) ou bairro geocodado (Google). Distância de RUA (Distance Matrix) com haversine de fallback.
- **Escala 100 lojas (obra 2026-06-12):** chamadas ao Google passam pelo cache `commerce.geo_cache` (0098; `geo-cache.ts`, read-through FAIL-OPEN, flag `GEO_CACHE` default on); Distance Matrix mede só as `GEO_ROAD_TOPK` (12) mais próximas em linha reta; partições mensais se criam SOZINHAS (0096, pg_cron dia 20) — julho/2026 não derruba mais a ingestão; 24 índices de FK (0097).
- `commerce.geo_resolutions` = dicionário bairro→cidade (624 bairros, 15 cidades; `resolve_neighborhood`). Cobertura: 7 lojas — rio(5)/niteroi(1)/itaborai(1), todas por cidade.
- Matriz = backstop universal quando nenhum parceiro atende. **✅ Decisão 06-27 ENTREGUE 06-29 — a matriz é "MAIS UMA LOJA"** (concorre IGUAL aos parceiros por proximidade+régua, retirada+entrega; anel entrega 40 km / retirada 15 km, Maps Petiti/SG). 3 tijolos FEITOS, atrás da flag `ROUTING_MATRIZ_AS_STORE` (exige `WHOLESALE_UNIFIED_STOCK` on, ambas ligadas no Coolify): **tijolo 1** = matriz entra no fim do `decideStoreForItemsGeo` SÓ quando nenhum parceiro está no pool (nunca fura a régua), estoque do GALPÃO via `getMatrizWholesaleStockQty`, dist por haversine ≤ maior anel; **tijolo 2** = baixa do galpão na venda; **tijolo 3** = retirada no galpão (`MATRIZ_MAPS_URL`). `buscar_produto` e `localizacao_loja` também enxergam a matriz como loja perto (≤15 km retirada normal; 15-40 km vira `retirada_so_longe` com distância+minutos de carro via Google — cliente decide). Prova de integração: `npx tsx --env-file=.env.pooler scripts/prova-matriz-loja-test.ts` (6/6; prova-geo 9/9 com flag off = não-regressão). ⚠️ Falta validar ENTREGA pela matriz ao vivo (frete escalonado 9,90/13/19). Ver memória `project_matriz_como_loja`.

## 7. Estado atual (LIVE em prod — **Deploy 07-10 CONFERIDO DE FORA: prod == main byte a byte**, etiquetas `?v=20260708-despesas1`+`logistica6` no ar; MATRIZ_LOGISTICS+MATRIZ_RECEIPT_AI LIGADAS pelo dono)
- **ABA COLABORADORES redesenhada (07-12b — desenho do dono com GPT, escolhido por ele; implementada pela casa) — SHIPADA, aguarda Deploy** (conferir `?v=20260712-colab1`; SEM migration/flag; menu lateral INTOCADO a pedido): obra 100% FRONT — backend 0124/0132 já tinha tudo. Drawer lateral direito pro cadastro (backdrop fecha), banner /entregas, 3 cards de contagem + pill revogados (getters derivados, zero estado duplicado), tabela com avatar de iniciais (cor determinística), busca + filtros Ativos|Revogados, **cadeado VISUAL do último proprietário** (select disabled+🔒 quando `colabOwnersAtivos<=1` — espelho da trava last_owner_required; servidor segue sendo o freio), função nasce vazia com validação, acesso nasce "Sem acesso" (menor privilégio — divergência CONSCIENTE do mock), senha com olhinho. ⚠️ **LIÇÃO: x-transition (classes OU inline) trava modal/drawer FECHADO em aba de fundo** — transição do Alpine depende de rAF que congela em background (mesma causa do screenshot travar no pane); drawer ficou SEM transição de propósito (comentado no HTML); vale pra todo modal futuro. Provas: preview 4230 pelo clique (cadastrou João Preview pelo drawer → contadores ao vivo 3→4 → revogar → filtro Revogados → reativar → busca → cadeado provado → console limpo → João deixado revogado), fiscal [OK] (183/282), **paridade 398 regravada DE PROPÓSITO** (12 props), 565 unit. Handoff: `docs/SESSAO_2026-07-12_ACERTO_REDESIGN_BOT_GPT_HANDOFF.md` (Parte 2). Memória: `project_colaboradores_matriz`.
- **REDESIGN da tela do BOT (dono + GPT, 07-11/12) + ACERTO 07-12 — SHIPADO, aguarda Deploy** (conferir `?v=20260712-botui1`; SEM migration/flag): o GPT repaginou a aba Bot em 4 sub-abas (Visão/Conversas/Vendas/Demanda; fila unificada com busca; mapa maior com % de conversão) DIRETO no main sem prova — a estrutura respeitou a casa (getters derivados no módulo-fábrica), mas: card "Resumo das últimas 48h" MENTIA (fallback do front usava o PERÍODO selecionado com rótulo 48h; campos `respondidas_bot_48h`/`espera_media_seg` nunca existiram no servidor), paridade quebrou (8 props sem regravar), `?v=` não bumpado, e o main JÁ reprovava no fiscal por herança do 0132/harden (`src/parceiro/route.ts` 1699>1678 congelado; `queries-colaboradores.ts` 324>300) + baseline de ROTAS esquecido pela sessão 0132. **Acerto:** `respondidas_bot_48h` REAL no servidor (`agent.turns` delivered, régua FIXA 48h, em `queries-bot-visao.ts`; front SÓ lê do servidor — calado = '—'; espera rotulada "Espera média da fila agora"); fatiagem `queries-colaboradores.ts`→113 + `queries-colaboradores-acesso.ts` 222 (ciclo de acesso 0132; 8 imports mortos removidos); extração `src/parceiro/route-login.ts` 70 (route.ts→1654; schemas ficam no route.ts e entram por INJEÇÃO — zero duplicação/ciclo; endpoint idêntico); teste `collaborators-access.test.ts` da 0132 ENTROU no git (tava untracked); baselines regravados DE PROPÓSITO (**paridade 386**, **rotas 103**). Provas: **prova-bot-tela 20/20 ×2** (B20 delta do delivered) · 565 unit · typecheck · fiscal **[OK]** · prova-colaboradores verde · preview **4230** pelo clique (login humano 0132; owner test `preview.dono` criado via bootstrap; ⚠️ `preview-matriz-server.ts` local precisa registrar `registerAdminLoginRoute` — sem ele o preview não loga pós-0132). ⚠️ Regra reafirmada: obra de QUALQUER LLM no main → rodar fiscais+provas ANTES do Deploy. Handoff: `docs/SESSAO_2026-07-12_ACERTO_REDESIGN_BOT_GPT_HANDOFF.md`. Memória: `project_tela_bot`.
- **VOZ DO CLIENTE (07-10b) — pesquisa cobre a ENTREGA DA MATRIZ (0131) + sino toca pra CANDIDATURA + nota no SCORE de saúde — SHIPADA, aguarda Deploy** (conferir `?v=20260710-rede2`; **0131 APLICADA test+prod**; **SATISFACTION_SURVEY=true LIGADA pelo dono DURANTE a sessão** → o trilho do PARCEIRO (0105) já está VIVO no deploy atual; o da MATRIZ entra neste push). Contexto: descoberta de que a pesquisa NUNCA rodou (banco 0 linhas desde sempre — flag nunca ligada; não "parou") e o disparo 0105 só via pedido de parceiro — a matriz virou loja e é quem MAIS entrega. (1) `0131` = `satisfaction_surveys.order_id` (→ commerce.orders) + índice único parcial; `dispatchMatrizDeliverySurveys()` = 2º trilho no MESMO worker (entrega da main 'delivered' recente + conversa → 1 pergunta; dedup ON CONFLICT; captura/expirador já serviam os dois trilhos; retirada da matriz FORA de propósito — sem marco `retrieved_at`); parceiro NÃO lê order_id (provado). (2) sino: candidatura deriva de `this.applications` (boot já carrega; zero backend novo), clique abre o modal; a COMISSÃO já tocava — só falta o dono preencher o alarme na Rede. (3) `getPainelRede` traz `satisfaction_avg/count`; check "Cliente satisfeito (X.X⭐, N)" (peso 20, ok ≥4) SÓ quando há amostra — não pune amostra vazia. Provas: **prova-satisfacao-matriz 8/8 ×2** + comissao-rede + financeiro-visao não-regressão + 522 unit + typecheck + fiscal + **paridade 373 IDÊNTICA** (props novas são de ITEM do array, não do objeto raiz — não regrava); preview 4229 pelo clique (sino badge→modal; 4.5⭐ no score; seeds limpos). ⚠️ Validar ao vivo pós-Deploy: entrega real da matriz → pergunta → nota; candidatura real → sino. Handoff: `docs/SESSAO_2026-07-10b_SATISFACAO_MATRIZ_SINO_SCORE_HANDOFF.md`. Memória: `project_pesquisa_satisfacao`.
- **AUDITORIA DE ABAS — REDE ✅ AUDITADA (07-10) nota ⭐⭐⭐⭐ + conserto do GRÁFICO shipado na hora** (`?v=20260710-rede1`; SEM migration/flag): a aba EXTRAI DE VERDADE do sistema do parceiro — código real vs PROD read-only bateu loja a loja (estoque 14 itens), seed em test provou a régua de venda realizada (2W 300 + porta 100 contam; delivery em curso NÃO conta e aparece "Em separação"), sweep criou comissão R$30 sozinho no GET (só do 2W realizado), Recebi quitou, alarme COBRAR disparou, raio null→25→null ponta a ponta (MESMA fonte do painel do parceiro e do motor), candidatura pública→fila→rejeitar + honeypot finge 201 sem gravar, prova-comissao 18/18, filtros/score/drill-down verdes com 42 unidades. **Conserto:** `redeSalesSeries/redeOrderSeries` somavam os 7 PRIMEIROS pontos da série do período (servidor manda o período INTEIRO, hoje por último) — venda de HOJE invisível no gráfico com o KPI do lado dizendo R$400; agora soma os ÚLTIMOS 7 alinhados pelo fim ('today' = 1 ponto honesto); paridade 373 IDÊNTICA. **Reportados (decisão do dono):** 🟠 5 lojas zz-teste ATIVAS em prod poluindo KPIs e roteamento (landmine do go-live); 🟠 `v_daily_metrics`/`v_clientes_pra_recuperar` SEM filtro de environment (Resumo + tela Bot misturam test quando test voltar a conversar — hoje test zerado; conserto = WHERE environment nas queries TS, sugerido pra auditoria do RESUMO); 🟡 "Resultado" da Rede é CAIXA (−compras) e o do painel do parceiro é COMPETÊNCIA (−custo da view) — alerta falso quando parceiro estoca; 🟡 funil de município sem pedido é descartado no merge (Maricá real tentou=1 invisível). **Faltas apontadas ao dono (ordem de dor):** mensalidade como lançamento · Recebi parcial + recibo do acerto · nota do cliente (0105 dormente) no score · sino cego pra comissão/candidatura · export contador. Fila: **Resumo · Colaboradores**. Handoff: `docs/SESSAO_2026-07-10_AUDITORIA_REDE_HANDOFF.md`. Memória: `project_auditoria_abas`.
- **FINANCEIRO rodada 2 (07-08c) — MODALIDADE DE DESPESA CADASTRÁVEL (0130) + FILTRO DE PERÍODO + fim da lista infinita — SHIPADA `4a7995c`, aguarda Deploy** (conferir `?v=20260708-despesas1`; **0130 APLICADA test+prod**; sem flag nova — mora atrás da MATRIZ_EXPENSES já ON). Pedido direto do dono: pedágio/alimentação caíam TODOS em "outros" e a lista de despesas ia virar rolo infinito. (1) `commerce.matriz_expense_categories` — 6 de fábrica `is_system` + as que o dono cadastra; PK (environment, slug) pois test/prod = MESMO banco físico; o CHECK fixo da 0120 SAIU e entrou **FK composta** (smoke da FK DENTRO da migration; zero grant parceiro provado); modalidade nova pelo PRÓPRIO select do form ("➕ Nova modalidade…" + prompt nativo), slug sem acento ("Pedágio"→`pedagio`), nome repetido→409 traduzido, **arquivar só custom** (chip ×; 'outros' é o fallback da IA — fábrica não arquiva; despesa antiga fica íntegra E com rótulo), recriar com o mesmo nome REATIVA; lançamento só em modalidade ATIVA (`INSERT…WHERE EXISTS` + casts `::env_t` — lição 42P08); **a IA de comprovante lê o vocabulário VIVO** (`buildReceiptSystemPrompt` monta o prompt com as ativas, FAIL-OPEN pras 6 — comprovante de pedágio passa a cair em "Pedágio" quando a modalidade existir). (2) `GET /admin/api/matriz/despesas?mes=YYYY-MM&categoria=slug` = **extrato do período** com soma própria na **MESMA régua do consolidado** (occurred_at, mês fuso SP — a prova CRAVA lista==visão, as duas telas nunca discordam); a-pagar e pago-no-mês seguem GLOBAIS (dívida não some ao filtrar); sem filtro o payload fica idêntico (visão/notificações intocadas). (3) a lista virou **"Despesas do período"**: input month default mês corrente SP (Intl sv-SE — `toISOString` viraria o mês mais cedo), select de modalidade, total+contagem do recorte, cap 200 com aviso honesto. Provas: **prova-despesas-categorias 25/25 ×2** (seeds ×2 envs · zero grant · normalização de acento · guard de ativa/arquivada · borda de fuso SP · régua cruzada · truncamento · prompt da IA puro), prova-financeiro-visao não-regressão, 522 unit, typecheck, fiscal, paridade **373** e rotas **99** (2 novas com AUTH) regravadas DE PROPÓSITO; preview **4228** ponta a ponta pelo clique (criar → lançar "7,10" com vírgula → filtrar mês/modalidade → arquivar → remover). ⚠️ Pooler de test LENTO (~2s) engana snapshot cedo no preview — o que parecia falha era sucesso carregando. Handoff: `docs/SESSAO_2026-07-08c_MODALIDADES_DESPESA_E_FILTRO_HANDOFF.md`. Memória: `project_auditoria_abas`.
- **AUDITORIA DE ABAS — FINANCEIRO ✅ AUDITADA (07-08b) nota ⭐⭐⭐⭐ + CONSERTO SHIPADO no mesmo dia ("resolve esses furos" do dono)** (pushado, aguarda Deploy; conferir `?v=20260708-frete1`; SEM migration e SEM flag nova): auditoria = prova visão **25/25 ×2** + fiado 14/14 + sino 12/12 (flags de prod) + preview 4227 ponta a ponta pelo clique (lançar à vista "55,90" com vírgula · a pagar → agenda vencido-primeiro · Paguei · Recebi fiado E comissão — o realizado do mês FICA · Remover = soft delete conferido no banco; ponto de equilíbrio EXATO 355 = 255,90÷margem 72%) + prod read-only (7 despesas da IA R$624,55 + 1 manual ok; atacado 335/varejo 99/capital 705). Integrações todas de pé (Vendas/Compras/Estoque/Logística/Comissão/Sino — cancelada some, capital = mesma conta, despesa da rota com ROTA-XXXX). **Conserto (3 peças):** (1) 🟠 **FRETE INVISÍVEL morto** — a gasolina da rota DESCONTAVA do lucro (despesa 0120) mas o frete recebido do cliente NÃO SOMAVA em perna nenhuma (0117 = só itens de propósito; a Logística sempre contou o frete no "rota se pagou" — as duas telas discordavam; provado em prod: R$13 fora vs R$507 de gasolina dentro): perna **"Frete de entrega"** no consolidado (`mes.pernas.frete.recebido`, SEMPRE presente, sem flag) com a régua da Logística `GREATEST(total−itens,0)` na MESMA janela/cancelado da perna do varejo (created_at mês SP, main, delivery); **faturamento e lucro do mês agora INCLUEM o frete** (entra cheio — o custo dele, a gasolina, já desconta nas despesas); barra ciano na tela + candidato no `finBarWidth`. (2) 🟡 **comissão defasada** — o sweep 0118 rodava só no boot do painel + GET da Rede (painel aberto há horas = "quem te deve" velho): o `GET /admin/api/matriz/financeiro` agora varre ANTES da visão, **FAIL-OPEN** (sweep caiu → warn e a visão serve; a visão SEGUE leitura pura — o sweep mora na PORTA, racional preservado); provado AO VIVO: venda 2W realizada sem lançamento → 1 GET do Financeiro → lançamento `open` R$50 nasceu E veio no "quem te deve" da MESMA resposta. (3) 🔵 giro com galpão zerado mostra "—" (era "0 dias" — ruído). Prova estendida test-first **30/30 ×3** (V9a-e: frete +10=110−100 · faturamento +110 · lucro sobe SÓ o frete, item sem custo não chuta · retirada fora · clamp 0 nunca negativo · cancelou→sai), prova-comissao-rede não-regressão, 522 unit, typecheck, fiscal, **paridade 365 IDÊNTICA** (payload novo, interface Alpine intocada — sem regravação). Fora DE PROPÓSITO: parse "1.500"→R$1,50 (padrão da casa INTEIRA em input de dinheiro — sessão própria), refresh 15s (padrão de todas as abas), Fase B (parcial/livro-razão/fiado varejo — banca de 4 antes). ⚠️ Pooler de test estoura EMAXCONNSESSION com 2 previews + prova — reciclar o preview PRÓPRIO. Fila da auditoria: **Resumo · Rede · Colaboradores**. Handoff: `docs/SESSAO_2026-07-08b_AUDITORIA_FINANCEIRO_E_FRETE_HANDOFF.md`. Memória: `project_auditoria_abas`.
- **AUDITORIA DE ABAS — LOGÍSTICA ✅ AUDITADA (07-08) nota ⭐⭐⭐⭐ + CONSERTO SHIPADO no mesmo dia ("vai" do dono + pedido extra do número da rota)** (pushado, aguarda Deploy; conferir `?v=20260708-logistica6`; **0129 APLICADA test+prod**): auditoria = prova **34/34 ×2** (flags de prod) + preview 4226 ponta a ponta (D+1 → remarcar reordena → trava rota vazia → abrir/pendurar → Entregue fecha pedido → Não-entregue devolve o galpão com ref certo no FILME 0128 → fechar rota lança gasolina → **"rota se pagou" EXATO** 209,90 = frete 9,90 + pneus 240 − rota 40 → funil do comprovante rejeita corrompido 415/aceita real). Prod: PED-0223 + 3 rotas + **IA leu 7/7 comprovantes (R$624,55 sozinha)**. **Conserto (3 peças):** (1) 🔴 o P1 morreu — bloco âmbar **"Reportadas pelo entregador — você decide"** na aba: o failed do portal (0125) SAI das Finalizadas, mostra o **motivo escrito pelo entregador** e ganha os 2 botões — **Recolocar na fila** (`requeueMatrizDelivery`: volta pending, solta da rota, limpa motivo) e **Confirmar não-entregue** (confirm nativo → `failMatrizDelivery`: cancela e o galpão VOLTA pelo caminho fdd9148); rotas `/recolocar`+`/falhou` já existiam (0125), era SÓ UI; sino atualiza junto (`loadSino`). (2) Comprovante em rota **FECHADA** ganhou status + miniatura + **"ler de novo"** (antes pending ficava preso — botão só existia na aberta; `loadReceiptThumbs` já varria as recentes). (3) **NÚMERO DA ROTA (0129)**: `trip_number` ROTA-XXXX no molde do PED (0057 — sequence + backfill CRONOLÓGICO por started_at + DEFAULT + UNIQUE + validação DENTRO da migration incl. zero grant parceiro); aparece no painel (rota na rua, badge do pedido, rotas recentes), no portal do entregador ("Minha rota — ROTA-XXXX") e na **DESCRIÇÃO das despesas** (fechamento E comprovante-IA) → despesa↔rota auditável no Financeiro; as 3 rotas reais de prod viraram ROTA-0001..0003 na ordem em que saíram. Prova ganhou **L25–L28b → 40/40 ×2** (limbo entra no bloco COM motivo e fora de abertas/finalizadas · recolocar volta pending+limpa+reaparece · recolocar 2× barra · confirmar cancela+galpão volta+2 · todo trip_number único ROTA-\d{4} · despesa do fechamento CARREGA o número); 522 unit, typecheck, fiscal, paridade **365** regravada DE PROPÓSITO (2 ações novas). Fora de escopo DE PROPÓSITO: rota otimizada (Uber cortado), aviso automático ao cliente (API paga), caixa do entregador (=frente de caixa). Memória: `project_auditoria_abas`.
- **AUDITORIA DE ABAS — ESTOQUE ✅ FECHADA (07-07) + FILME DO GALPÃO (0128) shipado no mesmo dia** (pushado, aguarda Deploy; conferir `?v=20260707-estoque1`; **0128 APLICADA test+prod** com SMOKE do trigger DENTRO da migration — se o trigger quebrar, a migration aborta): a auditoria deu ⭐⭐⭐⭐½ (zero bug; 8 funcionalidades verdes no preview) e o dono mandou resolver as 4 lacunas na hora. (1) **TRILHA DE MOVIMENTAÇÃO por TRIGGER** (`commerce.wholesale_stock_movements` + `log_wholesale_stock_movement` SECURITY DEFINER search_path fixo, padrão analytics da casa) — TODA mudança de qty/custo no galpão vira linha (op/before/after/delta GERADO/custo/source/reason/ref) venha do bot, balcão, atacado, compra, cancelamento, tela ou script; rótulo via `set_config` LOCAL (`app.galpao_*`) plantado nos caminhos: wrappers do painel (`queries-galpao-movimentos.ts` — definir/entrada/remocao), venda+cancelar atacado (`wholesale-stock.ts` ganhou `ref?`), compra+cancelar compra (fornecedores), varejo+cancelar varejo (`wholesale-stock-read.ts` DENTRO da folga do teto congelado 377/392 — bot `tools.ts` INTOCADO); sem rótulo = 'sem_rotulo' (a trilha NUNCA fura — fail-safe provado); NÃO é o livro-razão da Fase B (fonte da verdade segue o saldo; nenhuma baixa mudou; a Fase B mantém banca de 4). (2) **BAIXA MANUAL com motivo** (quebra/perda/uso/outro + detalhe) — `applyGalpaoBaixaManual` RECUSA acima do saldo (`baixa_maior_que_estoque:<tem>` → 409, msg "só tem N; se o sistema estiver errado, corrija pelo Definir"); não mexe no custo médio. (3) **resumo no topo da aba** (pneus/R$ parado/pra repor/zeradas — mesma conta do `capital_parado` do Financeiro, calculada da lista que a tabela renderiza). (4) **busca por texto/dígitos + ordenação zerou→repor→normal** (`stockRowsView`). UI: seção **Movimentação** (últimos 50, filtro por medida via botão "filme" na linha, rótulos pt-BR, custo só quando mudou) + form âmbar de baixa. Rotas `GET /admin/api/wholesale/stock/movimentos` + `POST .../stock/baixa` (auth; `mapWriteError` ganhou os códigos novos + `min_invalid` do 0126 que caía em 500). Consertos de percurso: guarda de corrida no filme (resposta velha descartada — o load geral do watch atropelava o filtro clicado) e teste unit do Decrement atualizado pro contrato novo (1ª query = rótulo). Provas: **prova-estoque-movimentos 18/18 ×2** (todas as bocas rotuladas + Σ deltas == saldo + fail-safe + baixa recusa sem rastro), sino-galpao 12/12, compras-cancel 18/18 (ganharam limpeza do filme), 522 unit, typecheck, fiscal; baselines regravados DE PROPÓSITO (paridade 363; rotas 97 com AUTH); preview 4226 ponta a ponta. Handoff: `docs/SESSAO_2026-07-07_ESTOQUE_FILME_GALPAO_HANDOFF.md`.
- **AUDITORIA DE ABAS — COMPRAS ✅ FECHADA (07-06c) + consertos SHIPADOS no mesmo dia** (pushado, aguarda Deploy; conferir `?v=20260707-compras1`; **0127 APLICADA test+prod**): a auditoria achou o trio "fácil errar, não vê, não desfaz" — compra INVISÍVEL (o front buscava a lista e jogava fora; sem tabela) e IRREVERSÍVEL (a 0114 já previa status 'cancelled', nunca construído), custo em branco virava R$0 calado (média do galpão desabou 23,64→19,26 ao vivo) e linha sem medida descartada em silêncio (tela R$170, registrou R$20). Consertos: tabela **"Últimas compras"** (soma de unidades + badge pago/a-prazo-vence/cancelada) + **CANCELAR compra** (`cancelWholesalePurchase` em `queries-fornecedores-cancel.ts` — reverte o galpão pelo INVERSO ponderado com clamps honestos; reversão INCONDICIONAL, simétrica da entrada; ranking/preço-por-medida/a-pagar se corrigem sozinhos) + **3 confirms no form** (custo R$0 · linha de fora · fornecedor repetido) + **arquivar fornecedor** (soft; dívida pendente FICA no a pagar) + erro de qtd traduzido. Prova nova prova-compras-cancel 18/18 ×2 (média reversa EXATA 20@25→10@20 + clamp + trilha); paridade 348 e rotas 95 (2 novas com AUTH) regravadas DE PROPÓSITO; preview 4225 ponta a ponta. ⚠️ Lição de prova: flag em prova exige `await import()` DINÂMICO no main (import estático é içado e mata o `process.env.X=` textual — provado empiricamente). Handoff: `docs/SESSAO_2026-07-06c_AUDITORIA_COMPRAS_E_CANCELAR_COMPRA_HANDOFF.md`.
- **AUDITORIA DE ABAS (07-06b, pedido do dono: aba a aba) — VENDAS ✅ FECHADA.** 6 provas de banco re-rodadas verdes com flags de prod (baixa do atacado · trava de oversell · fiado 0115 · cancelar atacado 0116 · custo/lucro varejo 0117 · cancelar varejo devolve galpão) + ciclo COMPLETO exercitado no preview 4224 pelo caminho do usuário: venda à vista baixou o galpão 5→3 e gerou recibo wa.me (telefone normalizado +55), venda FIADA caiu no "a receber" com vencimento e recibo "Fiado — vence", Recebi quitou (a receber zerou, venda virou paga), cancelar ×2 devolveu o estoque a 5 e zerou resumo/números do ranking (cadastro fica "nunca comprou" — correto, cadastro ≠ compra). **1 conserto de clareza shipado:** card Faturamento do varejo agora avisa **"só pneus — frete fora"** (provado no PED-0264: total 199,90 vs itens 189,90 — os R$10 são frete; a conta do 0117 exclui frete DE PROPÓSITO, mesma régua da comissão, mas a tela não explicava e a tabela ao lado soma COM frete). Observações não-bloqueantes na memória (refresh 15s não recarrega o bloco atacado — só clique na sub-aba/ação; cancelar usa confirm/prompt nativos). Veredito: **aba PRONTA pro dia a dia.** Memória: `project_auditoria_abas`.
- **Sessão 07-06b — TELA DO BOT no painel da matriz (campainha + funil de perdas + mapa + radar) — PUSHADA, aguarda Deploy** (conferir `?v=20260706-bot2`; SEM flag e SEM migration — tudo SÓ-LEITURA derivado dos sensores que já existem; furou a fila com o "vai" do dono). Aba **Bot** no menu vivo: (1) **campainha** — "cliente esperando resposta" (última msg do cliente sem resposta ENTREGUE — régua do stale-trigger INVERTIDA; grace 5 min, janela 24h, por `sent_at`) + "pediu humano" (fact `escalou`, 48h); roda no boot + refresh 15s em QUALQUER página, badge na aba, botão abre a conversa no Chatwoot; (2) **cards do período** (v_daily_metrics: conversas/conversão/custo/horário de pico; seletor hoje/7d/30d/mês); (3) **mapa da demanda** — malha IBGE 24 municípios da RM (`mapa-rm-dados.js`), camadas chamou/pediu/efetivou/faltou (facts municipio_entrega/pedido_criado/faltou_estoque × orders), paleta 07-06 (cinza = sem dado; UMA cor por assunto; mais escuro = mais forte), nomes invisíveis no hover/clique; (4) **radar de reposição** — "pediram e a Rede NÃO tinha" por medida × galpão. **Fatia 2 (mesmo dia, saiu do censo no analytics — o gerador 0102 grava 3 camadas e a tela só usava 1):** (5) **funil do período com perdas POR MOTIVO** (classifications `stage_reached`+`loss_reason`, que JÁ existiam: achou caro/citou concorrente/sumiu após frete/após bairro/desistiu cedo/foi pro humano — front acumula o funil, chips de perda); (6) cards **faturamento do bot + ticket médio + resposta média** (colunas da v_daily_metrics que a tela ignorava); (7) **"da boca do cliente"** (linguistic_hints por conversa distinta: preço/concorrente/parcelado-fiado/garantia/instalação/pressa — rotulado "termômetro, não balança"); (8) **medidas mais pedidas × galpão** (fact `medida_consultada` — reposição preventiva, ao lado do radar). Visão mora em `queries-bot-visao.ts` (fatiado por assunto; campainha ficou em `queries-bot.ts`). Rotas `GET /admin/api/bot/campainha`+`/visao` admin-only (zero grant parceiro — conversa de cliente NUNCA vai pro parceiro). ⚠️ Lição: módulo novo do painel EXIGE entrada na lista fixa de `route-static.ts` — 404 no módulo derruba o Alpine INTEIRO (painel branco); pego no preview. Baselines regravados DE PROPÓSITO (paridade 341→345; rotas 93). Provas: prova-bot-tela **19/19 ×2**, 522 unit, sino não-regressão, preview 4224 ponta a ponta. Obra iniciada em OUTRA conta que esgotou créditos no meio — retomada por leitura de disco+git, zero perda. NÃO entrou de propósito: nota de satisfação (pesquisa dormente, 0105 off), moto mais consultada, detalhe de tokens. Handoff: `docs/SESSAO_2026-07-06b_TELA_BOT_HANDOFF.md`. Memória: `project_tela_bot`.
- **Sessão 07-06 — SINO VIVO + ESTOQUE MÍNIMO do galpão (0126) + RECIBO de atacado — PUSHADA `63ee505`, aguarda Deploy** (conferir `?v=20260706-sino1`; sem flag nova). Saiu do 360 do painel: (1) o **sino** do topo (morto desde o desenho) agora DERIVA notificações REAIS — `GET /admin/api/matriz/notificacoes` (admin-only) agrega entrega failed aguardando o dono (guard main), fiado vencido, contas a pagar vencidas e **galpão pra REPOR**; comissão ≥ alarme o front soma sozinho (localStorage); lida = assinatura em localStorage; clique navega pra aba; refresh 15s; (2) **0126 APLICADA test+prod**: `min_quantity` no galpão (NULL = sem alerta, opt-in) — badge REPOR âmbar na aba Estoque, "+ Entrada" PRESERVA o mínimo, só leitura (nenhuma baixa trava); antes só avisava no ZERO; (3) **Recibo** da venda de atacado via wa.me (texto pronto com itens/total/fiado+vencimento; `listWholesaleSales` ganhou telefone+itens; sem telefone = sem botão); (4) card morto "insights da Organizadora" REMOVIDO do Resumo. Baselines regravados DE PROPÓSITO (paridade 315 props — `notificacoes` virou getter; rotas 88 — a nova com AUTH). Provas: prova-sino-galpao 12/12 ×2, 522 unit, não-regressão galpão/cancelar/fiado, preview 4223 ponta a ponta. **Fila combinada com o dono (cada uma sessão própria): Fase B fiado PARCIAL (banca de 4 ANTES) → frente de caixa do vendedor (seguranca revisa desenho ANTES) → Relatórios/export contador.** Memória: `project_sino_minimo_recibo`.
- **Sessão 07-04 — COLABORADORES DA MATRIZ (0124) + PORTAL DO ENTREGADOR (0125) — PUSHADA, aguarda Deploy** (ligar `MATRIZ_ENTREGADOR_PORTAL` no Coolify quando o dono quiser operar — nasce OFF = portal `/entregas` invisível/404; exige `MATRIZ_LOGISTICS` on). **Base primeiro (decisão do dono):** aba **Colaboradores** no painel da matriz (menu vivo, `?v=20260704-colaboradores1`) — cadastra vendedor/entregador reusando a pessoa da porta única (0095), tabela nova `network.matriz_collaborators` SEPARADA dos parceiros (matriz não é partner_units; zero grant provado). Fatia 1 = só cadastro: colaborador NÃO loga em lugar nenhum ainda (sem vínculo de loja → authenticatePersonGlobal null). **Depois o PORTAL DO ENTREGADOR** (`src/admin/entregador/`, front `painel/public/entregas.html`+`entregas.js` mobile): o colaborador job='entregador' loga em `/entregas` no celular e opera SÓ a entrega — fila do dia (D+1), abrir a rota DELE (km), saiu/entregue(pagamento COD)/não-entregue, fechar (km/gasolina/foto lida pela IA). Sessão própria `es_` (0125 `matriz_staff_sessions`, TTL 7d; revogar o colaborador mata na hora via JOIN). **Segurança é o coração (não há RLS — tudo é o WHERE):** card financeiramente CEGO (servidor nem seleciona custo/lucro/despesa); posse da rota (`courier_collaborator_id`) no WHERE de TODA escrita; login 401+timing único; flag off = 404 invisível; **NÃO-ENTREGUE só REPORTA** (failed+motivo — o dono confirma/cancela no painel via `requeueMatrizDelivery`/`failMatrizDelivery`; o portal NUNCA cancela nem devolve galpão, pq `cancel_manual_order` não tem freio de permissão). **Rito seguido:** seguranca revisou o DESENHO (FIX-ANTES pré-código) → banca de 2 no CÓDIGO (seguranca achou 1 timing-oracle no login → faltava `password_hash IS NOT NULL`, CONSERTADO + travado na prova; banco = SHIP, índice único global de "1 rota aberta/entregador" provado ao vivo). Provas: prova-matriz-colaboradores 18/18 ×2, prova-entregador-portal 25/25 ×2, 522 unit, prova-logistica 20/20 (não-regressão), typecheck; preview 4220 (colaboradores) + 4221 (`/entregas`, teste.entregador/senha456) ponta a ponta (login→abrir rota→entregue Pix→trilha gravou o nome). **0124+0125 APLICADAS test+prod.** Fila: frente de caixa do VENDEDOR (mesma base 0124). Memórias: `project_colaboradores_matriz`, `project_entregador_portal`.
- **Sessão 07-03b — LOGÍSTICA DA MATRIZ (0121) — PUSHADA `7022462` + card `9e3fd56` + "a rota se pagou?" (07-03c) + vínculo pedido↔rota (07-03d) + agendamento D+1 (07-03e), aguarda Deploy** (conferir `?v=20260703-logistica5`; ligar `MATRIZ_LOGISTICS` + `MATRIZ_RECEIPT_AI` no Coolify quando o dono quiser operar). Card da entrega com **WhatsApp/Waze/Maps** do entregador (deep-links custo zero portados do parceiro; WhatsApp só com telefone — pedido do bot tem via contact, walk-in esconde; provado no preview 4218). Aba Logística nos moldes do parceiro + o extra do dono: **rota do entregador** (decisões dele 07-03: diário por SAÍDA; acesso só-rota do entregador = fatia C futura) com km inicial/final, gasolina e **comprovantes que a IA lê e lança como despesa SOZINHA** no 0120 (`receipt-ai.ts`: nunca inventa — sem confiança ≥0.7 vira 'unreadable' e avisa; erro de rede fica 'pending' com "ler de novo"). Entregas da main: card → Saiu → Entregue (fecha o pedido; régua 0117 intocada) / **Não entregue = cancela e o galpão VOLTA** (caminho fdd9148, atômico). Fechar rota com gasolina lança despesa 'combustivel'; **ANTI-DUPLA nas duas ordens** (comprovante-despesa ganha do fechamento; leitura posterior COLA na despesa existente — P1 da banca consertado, FOR UPDATE serializa). Guard `MAIN_DELIVERY_GUARD` em toda leitura/escrita (pedido de parceiro inalcançável). Upload no funil blindado da foto (magic bytes+re-encode+EXIF fora), miniatura AUTENTICADA via blob (img puro = 401), cap 50/rota, todas as rotas com auth, zero grant parceiro (provado em prod). **Banca de 4 ANTES do push: 3×SHIP + 1 FIX-ANTES consertado.** Provas: prova-logistica 20/20 ×2 runs (determinística), 522 unit, preview 4217 ponta a ponta (despesa apareceu no Financeiro). **Adendo 07-03c — "A ROTA SE PAGOU?" (banca de 2 matriz+banco = 2×SHIP zero P1):** a rota fechada mostra `sobrou/faltou R$ X — frete + pneus − rota` (frete = total−itens, o bot embute no total; lucro = régua 0117 custo congelado — item sem custo AVISADO "lucro parcial", nunca chuta; SÓ entrega delivered conta, falha/cancelada fora; gasto = fechamento ∪ comprovantes IA SEM dobrar, dedup provado no banco vivo; despesa apagada no Financeiro some da rota na hora). `resumo`+`despesas_total` no tripSelect de `getMatrizLogistica` (`queries.ts`), `rotaResumo()` no front só soma/formata; **0122 índice parcial `orders(environment,trip_id)` APLICADA** (P2 do banco). Prova 25/25 ×2 (L16a-d + L14d anti-dobro do linked), 522 unit, typecheck. **Adendo 07-03d — VÍNCULO PEDIDO↔ROTA (decisão do dono, opção 2: pendurar + travar vazia):** o vínculo na abertura (checkbox "Entregas que vão na rota") já existia; o furo era a tela prometer "pendurar depois" sem código atrás. Agora **`attachOrderToMatrizTrip`** pendura uma entrega numa rota JÁ ABERTA (botão "Pôr na rota" nas entregas em aberto — herda o entregador, marca dispatched, mesmo `MAIN_DELIVERY_GUARD`; atômico via EXISTS(rota open) no próprio UPDATE) e **`openMatrizTrip` NÃO abre mais vazia** (count 0 → `trip_needs_delivery`, ROLLBACK desfaz a trip; botão "Abrir rota" trava sem entrega marcada). Endpoint `POST /admin/api/logistica/rotas/pendurar` (auth do dono, zod UUID). Auto-revisão de segurança do caminho novo: isolamento provado (fora da main não pendura), não rouba pedido de outra rota (`trip_id IS NULL`), só rota aberta, 1 clique=1 POST (`:disabled` trava double-submit). **`?v=20260703-logistica4`.** Prova **30 checagens ×2** (L17-L20), 522 unit, typecheck, verificação no preview 4219 (clique pendura ponta a ponta, botão Abrir trava vazio). **Adendo 07-03e — AGENDAMENTO D+1 (regra do dono: pedi hoje, sai amanhã):** `0123` (`commerce.orders.scheduled_delivery_date` DATE nullable — SEM lock: o D+1 padrão vive na LEITURA `COALESCE(scheduled_delivery_date, created_at+1 fuso SP)`, bot INTOCADO; a coluna guarda só a EXCEÇÃO quando o dono REMARCA). `rescheduleMatrizDelivery` (`POST /admin/api/logistica/entregas/remarcar`, guard main + só pending/dispatched — remarcar NÃO toca status, provado); fila ordenada por data (atrasada/hoje primeiro); front badge "pra amanhã/hoje/atrasada" + seletor de data "sai em" nas entregas pending. `?v=20260703-logistica5`. Prova **34 checagens ×2** (L21 D+1 + L22 remarcar + L23/L24 guard), 522 unit, typecheck, preview 4219 (badge "pra amanhã", remarcar muda só a data). **0123 APLICADA em test E prod.** ⚠️ Validar ao vivo pós-deploy: comprovante REAL lido pela IA (chave OpenAI só no Coolify). Handoff: `docs/SESSAO_2026-07-03b_LOGISTICA_MATRIZ_HANDOFF.md`. **Frete: a MATRIZ já cobra por DISTÂNCIA (`matriz-freight.ts`: ≤15→9,90 / ≤25→13 / >25→19); o PARCEIRO é que é fixo R$9,90.**
- **Sessão 07-03 — Financeiro da matriz (Onda 1) + banca de conciliação.** Aba **Financeiro** do painel da matriz PROMOVIDA pro menu vivo e virou tela RICA (`c6dfb18`, LIVE): consolidado do mês das 3 pernas − despesas (0120, flag `MATRIZ_EXPENSES` ON), "quem te deve" (fiado do atacado + comissão, com Cobrar/Recebi), agenda a pagar (fornecedor+despesa, vencido primeiro), indicadores com guarda null (capital parado, giro, fiado %, ponto de equilíbrio) — tudo **SÓ LEITURA** (`getMatrizFinanceiroVisao` em `src/admin/painel/queries.ts` + `GET /admin/api/matriz/financeiro`; a visão NÃO roda o sweep da comissão, de propósito). Despesas moram na aba FINANCEIRO, não em Vendas (correção do dono 07-03). Depois, **banca de 4** (matriz+banco+parceiro+seguranca) conciliou Financeiro×Vendas×Estoque×Rede: **1 furo real + 2 robustezes, consertados em `fdd9148` (LIVE no Deploy 07-03**; ⚠️ falta validar ao vivo: cancelar uma venda de varejo e ver o galpão voltar): (1) 🔴 cancelar VAREJO da matriz não devolvia o galpão → agora a baixa grava trilha `audit.events 'matriz_galpao_decrement'` com o DELTA REAL e `applyMatrizGalpaoReturn` devolve GUIADO PELA TRILHA (atômico com o cancelamento, balcão+bot; sem trilha → não inventa; idempotente; sob clamp devolve só o que saiu); (2) fiado % do mês na mesma base + teto 100%; (3) giro por janela de 30 dias corridos. Provas: financeiro-visao 25/25, cancel-varejo-galpao 11/11, 522 unit, typecheck ✓, preview 4215. Segurança: ZERO grant do parceiro em tudo (provado em prod). ⚠️ A lista de flags que o dono colou 07-03 NÃO tinha as 8 do motor (`ROUTING_GEO`, `ROUTING_PROXIMITY_FIRST` etc.) — confirmar que estão =true no Coolify. **5 dos 7 parceiros com `commission_percent` NULL = TESTE** (palavra do dono, zero ação). Handoff: `docs/SESSAO_2026-07-03_FINANCEIRO_MATRIZ_ONDA1_E_BANCA_CONCILIACAO_HANDOFF.md`.
- **Deploy 07-02 (`9044b2a`) carregou:** FIADO do atacado LIVE (§11); trava de oversell do ATACADO (avisar+confirmar) live; **frete de entrega pelo PINO** (`DELIVERY_FREIGHT_FROM_PIN` LIGADA 07-02, palavra do dono — furo #696: bot media a retirada pelo pino mas exigia endereço escrito só pra COTAR a entrega; ⚠️ falta validar ao vivo repetindo o roteiro do #696). No Deploy 07-03 subiram também CANCELAR venda de atacado (`ed82fa5`), FATIA 2 lucro do varejo (`91a3694`) e COMISSÃO como LANÇAMENTO (`1eae2aa`, banca 4×SHIP) — flags `WHOLESALE_MATRIZ_RETAIL_COST` + `NETWORK_COMMISSION_LEDGER` + `WHOLESALE_MATRIZ_OVERSELL_GUARD` LIGADAS (lista do dono 07-03) (§11). Handoffs: `docs/SESSAO_2026-07-02_FRETE_PINO_FIADO_E_CANCELAMENTO_ATACADO_HANDOFF.md` + `..._2026-07-02b_FATIA2_...` + `..._2026-07-02c_COMISSAO_LANCAMENTO_HANDOFF.md` + `..._2026-07-02d_RETOMADA_ALARME_BANCA_E_PUSH_HANDOFF.md`.
- **Matriz como loja LIVE** (sessão 06-29, ver §6 + handoff `docs/SESSAO_2026-06-29_MATRIZ_COMO_LOJA_E_FIXES_BOT_HANDOFF.md`). Banco de teste ZERADO 06-29 (conversas+pedidos+analytics limpos; estoque/cadastros/galpão preservados) — pronto pra cliente real após a faxina do go-live.
- **Fixes do bot 06-29 (todos LIVE):** (1) `productNudge` em `agent.ts` força `buscar_produto` quando o cliente manda uma medida (regex) — matava o "Tenho sim" de cabeça; (2) nome do cliente só na saudação (não em todo turno — soava robótico); (3) anti-eco em `agent.ts` descarta resposta idêntica ao turno anterior (cliente manda 2 msgs seguidas → não duplica); (4) `localizacao_loja`/`buscar_produto` mostram distância + minutos de carro da matriz (Google Distance Matrix, `matrizRoadInfo`/`cachedMatrizRoadInfo`, cache invalida entradas sem `durationMinutes`).
- **Pino-first** funciona ponta-a-ponta: cliente manda o pino → bot resolve cidade (reverseGeocode) → acha a loja → reserva. Validado ao vivo (PED-0034). O nudge determinístico (`agent.ts`) força o bot a usar a tool quando há pino.
- **Saudação-espelho:** só cumprimentou → "Como posso te ajudar?"; chegou com pedido → cumprimenta + pede localização. ("borracharia mais perto de você").
- **Bot não engole a 2ª mensagem** (06-27; deployado, falta teste ao vivo): a trava anti-requentado (`stale-trigger.ts` + `worker.ts`) olha QUAL mensagem já foi respondida (`agent.turns.status='delivered'` — `delivered_message_id` é coluna MORTA), não o relógio da última resposta. Cliente manda "oi"+"tem pneu?" e o bot responde o pneu (antes engolia quando a saudação saía atrasada). Conserto por CÓDIGO, prompt intocado.
- **Gatilhos de conversão:** proximidade ("tá a ~X km", ≤5/5-10/>10), escassez (estoque 1-3 → reserva), imediatismo (horário se preenchido + "reservado").
- **Pickup-to-partner** (retirada reserva no parceiro), **camada GEO**, **consentimento de retirada longe**, **gancho de instalação** (taxa à parte) — tudo live. Migrations 0089-0093 aplicadas.
- **Proximidade-primeiro COMPLETA — Fases 0/1/2/3 TODAS LIVE em prod** (commit `554b6d2`, 2026-06-09; ver §8): retirada E entrega por distância ligadas; entrega entra só se raio preenchido E dist ≤ raio. Os 7 raios foram preenchidos com valores de **TESTE** (pra induzir o bot ao erro) — trocar pelos reais depois dos testes. Dados de teste do bot LIMPOS (PED-0034 etc.).
- **O QUE VAMOS FAZER (roadmap, do mais próximo ao maior):**
  1. **Validar a Fase 3 ao vivo:** apagar as conversas no Chatwoot (senão o webhook recria) → rodar o roteiro de 6 pinos que estressa o raio (Centro→Méier, Barra→matriz, Barra retirada→atende, Caxias→entrega fora da cidade, etc.) → depois **trocar os raios de teste pelos REAIS** de cada parceiro.
  2. Fase 2 menor: aviso no login forte; cadastro "Novo parceiro" coletar o raio.
  3. Alerta na matriz pra pedido acima do raio no caminho SEM coordenada (decisão 2 do dono — ficou de fora da Fase 3).
  4. Antigos: preencher **horário das 6 lojas** vazias; **SEC-002** (autorização sem RLS, sessão dedicada — §9). (Senha temp da zz-teste-copacabana: trocada em 06-10, com o dono; resetar = `scripts/resetar-senha-parceiro.cjs`.)

## 8. Proximidade-primeiro (derruba o "muro da cidade") — COMPLETA, Fases 0/1/2/3 LIVE
**Furo:** o roteamento filtrava por CIDADE antes da distância → cliente de Caxias a 9 km de Madureira caía na matriz. **Virada:** com coordenada, rotear por DISTÂNCIA (anel+estoque+régua decidem); cidade vira plano B; na entrega, o RAIO declarado por loja é o teto. Tudo atrás da flag **`ROUTING_PROXIMITY_FIRST`** (default OFF no código; **o dono ligou =true no Coolify**). Rollback = flag false (desliga entrega+retirada por proximidade juntas).
- **Fase 0+1 (fundação + RETIRADA) — LIVE.** Migration 0093 `delivery_radius_km`; `resolveUnitCandidatesByProximity` (sem muro de cidade); retirada por proximidade ligada.
- **Fase 2 (coleta do raio) — LIVE** (2026-06-09): campo "até quantos km você entrega?" no **painel** (aba Atendimento) + editor "Raio de entrega (Rede)" na **matriz** (página 'unidade'). Gravam `network.partner_units.delivery_radius_km` (fonte única; matriz só edita quem faz entrega — trava de autonomia).
- **Fase 3 (ENTREGA por proximidade) — LIVE** (commit `554b6d2`, 2026-06-09): com a flag on, a entrega usa o pool por proximidade e a loja só entra se `delivery_radius_km` preenchido E dist ≤ raio (`filterByModeAndRadiusPresence` + `passesDeliveryRadius` em `geo-routing.ts`); a cobertura de bairro só vale no caminho por cidade (flag off). A BUSCA segue a mesma régua (nunca diverge do pedido). Aba "Área de entrega" do painel SIMPLIFICADA (só município/plano B; bairros saíram). Provas: prova-proximidade 11/11, prova-geo 9/9, 337 unit. **Os 7 raios estão com valores de TESTE** (induzir erro) — trocar pelos reais após validar ao vivo.
**Detalhe:** `docs/SESSAO_2026-06-09d_FASE3_ENTREGA_RAIO_HANDOFF.md` (Fase 3) + `..._09c_FASE2_PAINEL_MATRIZ_HANDOFF.md` (Fase 2) + `..._09b_PROXIMIDADE_HANDOFF.md` (plano/Fase 0-1).

## 9. Segurança
- **SEC-001 RESOLVIDO** (bot não vaza pedido de outro cliente — amarra contact_id).
- Auditoria RLS: nada de cliente/financeiro vaza entre parceiros (partner_* têm RLS+policy).
- Aberto: SEC-002 (autorização sem RLS) — fix arriscado, sessão dedicada. Ver `docs/SEGURANCA.md`.
- Revisar caminho que expõe dado de cliente/parceiro ANTES de mexer (usar agente `seguranca`).

## 10. Onde achar mais
- **Handoffs de sessão:** `docs/SESSAO_*_HANDOFF.md` (no git — o histórico detalhado).
- **Manual de contratos por seção:** `SECOES/` (ex.: ESTOQUE.md).
- **Memória privada do orquestrador** (local, NÃO no git): `~/.claude/projects/C--Farejador-agente/memory/` — `project_*` (cada frente), `feedback_*` (estilo do dono), `parceiro_*`. É o detalhe vivo; este CLAUDE.md é a destilação compartilhada.

## 11. Atacado da Matriz (o "filé" do dono — LIVE)
O dono é **atacadista de pneu usado**: vende pros borracheiros (o Farejador é o pump pra eles venderem e RECOMPRAREM dele). A matriz tem o ciclo de atacado em `commerce.wholesale_*` — dado **SÓ da matriz, ZERO grant pro parceiro** (regra de ouro provada nas migrations). UI na aba **Vendas → Atacado** do painel da matriz (`painel/public/`).
- **LIVE em prod (main `c6dfb18`, Deploy conferido 07-03):** **Fases 1-4** (venda pro borracheiro + ranking de recompra · estoque do galpão por MEDIDA + busca · custo+lucro · custo médio ponderado · medida em NÚMEROS larg/alt/aro `0113` validada contra o catálogo via `tireSizeKey`), **BAIXA do atacado** (`WHOLESALE_STOCK_DECREMENT` ON — confirmada em prod com venda real 5un: 35→30), **BAIXA do galpão no VAREJO da matriz** (bot+balcão, `applyMatrizGalpaoDecrement`, `WHOLESALE_MATRIZ_DECREMENT` ON), **fornecedores/compras** (`0114`), **trava de oversell do atacado** (avisar+confirmar + lock de linha) e o **FIADO** (`0115` + flag `WHOLESALE_FINANCE` ON — bloco "Financeiro do galpão": a receber/a pagar/vencidos + botão Recebi/Paguei + seletor à vista×fiado nos forms de venda e compra; ⚠️ falta validar ao vivo com venda fiada real).
- **LIVE (Deploy 07-03, `ed82fa5`):** **CANCELAR venda de atacado** (`0116` APLICADA — trilha cancelled_at/by/reason; DEVOLVE o estoque como espelho exato da baixa; cancelada some SOZINHA do fiado/ranking/resumo; tabela "Últimas vendas" com badge + botão Cancelar + aviso forte se venda paga; SEM flag, aditivo; provado 13/13; ⚠️ assimetria do clamp em oversell documentada no código — correção real = livro-razão/Camada 2, adiada).
- **LIVE (Deploy 07-03, `91a3694`, fatia 2):** **LUCRO REAL DO VAREJO da matriz** (`0117` — bot+balcão+manual da 'main' CONGELAM o custo médio do galpão em `order_items.matriz_unit_cost`; flag `WHOLESALE_MATRIZ_RETAIL_COST` LIGADA 07-03; card "Financeiro do varejo" Faturamento/Custo/Lucro do SERVIDOR + banner de item sem custo; recorte **Tudo × Esse mês** nos cards do varejo E do atacado, fuso São Paulo; endpoint `/admin/api/varejo/resumo`; provado 15/15 + 519 unit + preview).
- **LIVE (Deploy 07-03, `1eae2aa`; banca de 4 especialistas = 4×SHIP):** **COMISSÃO como LANÇAMENTO** (`0118` — `network.commission_entries`, ZERO grant pro parceiro; regras do dono 07-02: nasce quando a venda 2W REALIZA (régua 0077/0090) · cancelou → estorna sozinho (pós-paga = "acerto por fora") · % da FICHA congelado no lançamento · FRETE FORA da base (comissão só sobre os pneus); VARREDURA idempotente no GET da tela (sem gancho no fluxo do parceiro/bot); bloco "Comissões a receber" na página Rede (UM Recebi por LOJA com total ACUMULADO; extrato lançamento-a-lançamento ESCONDIDO atrás de "ver extrato"; **alarme COBRAR**: dono define R$ X no localStorage → loja com aberto ≥ X pisca badge + botão "Cobrar no WhatsApp" via wa.me com msg pronta — pedido do dono 07-02) + **editor do modelo comercial** na página da unidade (pendência de 06-01 QUITADA, com audit.events); flag `NETWORK_COMMISSION_LEDGER` LIGADA 07-03; provado 18/18 (incl. frete fora) + 519 unit + preview (alarme + Recebi validados no 4213 pelo dono); com a flag on, o card antigo "A receber da rede" (Resumo+Rede+drill-down) LÊ O LIVRO em vez da estimativa % × vendas (furo apontado pelo dono: quitava e o card não descontava); `?v=20260703-comissao3`; mensalidade fica pra próxima fatia — decisão do dono).
- **LIVE (Deploy 07-03, `c6dfb18`+`fdd9148`+`a50cfe7`) — FINANCEIRO da matriz Onda 1 + consertos da banca + aba ESTOQUE** (ver §7): despesas (`0120`, `commerce.matriz_expenses`, flag `MATRIZ_EXPENSES` ON) + visão consolidada só-leitura das 3 pernas; `0119` (índice parcial 2w em partner_orders + trava física de grant no livro de comissão) APLICADA; consertos da banca — cancelar VAREJO devolve o galpão guiado pela trilha (melhor que o 0116 do atacado, cujo clamp assimétrico fica pra Camada 2), fiado % mesma base+teto, giro 30 dias; **aba Estoque própria** — o bloco do galpão saiu de Vendas → Atacado pro menu vivo (entre Compras e Financeiro; mesmo dado, `loadAtacado` no `$watch` de `currentPage`; a venda continua vendo o "✓ em estoque" e o autocomplete).
- **Modelo:** estoque por **MEDIDA simples** (sem dianteiro/traseiro — decisão do dono); custo **MÉDIO ponderado** (botão "+ Entrada" soma a compra e recalcula a média; "Definir" grava direto). A venda **congela o custo** (snapshot) e o lucro = (preço−custo)×qtd; card Faturamento/Custo/Lucro.
- Migrations **0110–0121** (0114 fornecedores; 0115 fiado; 0116 trilha de cancelamento; 0117 custo congelado do varejo; 0118 livro de comissão; 0119 índice+trava de grant; 0120 despesas; 0121 logística — todas APLICADAS). Detalhe: `docs/SESSAO_2026-07-03_FINANCEIRO_MATRIZ_ONDA1_E_BANCA_CONCILIACAO_HANDOFF.md` + `..._2026-07-02_FRETE_PINO_FIADO_E_CANCELAMENTO_ATACADO_HANDOFF.md` + `..._2026-06-27_GALPAO_*` + `..._2026-06-22_ATACADO_*`.
- ⚠️ Lição: subquery no INSERT reusando `$1` env_t dá **42P08** (text vs env_t) → buscar à parte. Ao mexer no `app.js`/`css` do painel: **bumpar o `?v=`** no `index.html` (cache-bust).
- **Roadmap (ordem de dor):** **Fase B do Financeiro — o coração** (livro-razão de caixa ÚNICO + pagamento PARCIAL "recebi R$200 por conta" + saldo/extrato entrou×saiu + projeção 30d; resolve os resíduos da banca — Camada 2 do galpão, clamp assimétrico do atacado; migrations 0122+; **gate = banca de 4 ANTES da 1ª linha**; planta `docs/PLANO_FINANCEIRO_MATRIZ_ROBUSTO_2026-07-02.md`); **Logística fatia C — acesso SÓ-ROTA do entregador** (decisão do dono 07-03; login próprio sem ver financeiro; fundação = porta única 0095/0100; zona de auth → `seguranca` revisa antes); **mensalidade como lançamento** (⚠️ banca 07-02: sair ANTES de ativar modelo monthly/hybrid pra parceiro real — mensalidade é por competência/mês, desenho próprio, não pendurar no sweep de vendas); **validações ao vivo pendentes:** Logística+IA de comprovante (pós-deploy, ligar flags), venda fiada real, frete pelo pino (roteiro #696), ENTREGA pela matriz (frete 9,90/13/19), `WHOLESALE_MATRIZ_OVERSELL_GUARD` (ligada 07-03, era dormente); faxina de chaves (combinada 07-02). Adiados DE PROPÓSITO: PEPS por fornecedor.
